
sudo: required

dist: trusty

language: cpp

services:
  - mongodb

before_script:
  - sleep 15

compiler:
  - gcc

before_install:
  - sudo pip install cpp-coveralls


install:
  - cd ApplicationServer
  - chmod +x install.sh
  - sudo ./install.sh
